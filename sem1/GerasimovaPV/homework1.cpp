#include <iostream>

float sSquare(float side)
{
	return side * side;
}

float sTrapezoid(float a, float b, float h)
{
	return (a + b) / 2 * h;
}

//int main()
//{
//	int a, b, h, n;
//	std::cin >> a >> b >> h >> n;
//	std::cout << sTrapezoid(a, b, h);
//	std::cout << sSquare(n);
//}
