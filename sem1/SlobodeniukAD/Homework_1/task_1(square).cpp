#include <iostream>

int main() {
  int area, a;
  std::cout << "Enter the length of the side of the square:";
  std::cin >> a;
  area = a * a;
  std::cout << "Square:" << area << std::endl;

  return 0;
}
